

name: 'dbt_jaffle_shop'
version: '1.0.0'
config-version: 2

profile: 'dev'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  
clean-targets:         
  - "target"
  - "dbt_packages"


models:
  dbt_jaffle_shop:
    schema: dbt_pavan
    +query_tag: _by_pavan
    
    staging:
      +materialized: view


