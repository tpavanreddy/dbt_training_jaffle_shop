version: 2

sources:
  - name: src_jaffle_shop
    database: raw
    schema: jaffle_shop

    tables:
      - name: customers
      - name: orders
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 200, period: hour}
  
  - name: src_jaffle_shop_strip
    database: raw
    schema: stripe

    tables:
      - name: payment
